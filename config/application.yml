app:
  config:
    client:
      peerId: 12345
      url: http://localhost:8089/
    handler:
      numberOfAttempts: 3
      messageSenderTimeout: 2000
    scheduler:
      period: 500
      awaitTerminationMillis: 1000
      threadNamePrefix: bot-sch
    creeds:
      groupId: 67890
      groupToken: test_token
